{
  "name": "ekstacy-backend",
  "version": "1.0.0",
  "description": "Secure backend for Ekstacy Figma plugin grammar checking",
  "main": "api/grammar-check.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Build complete - serverless functions ready'"
  },
  "dependencies": {},
  "engines": {
    "node": ">=18.x"
  }
}